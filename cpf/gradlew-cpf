#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")"
if [[ -n "${CPF_JAVA_HOME:-}" ]]; then
  export JAVA_HOME="$CPF_JAVA_HOME"
elif [[ -z "${JAVA_HOME:-}" ]]; then
  export JAVA_HOME="$(/usr/libexec/java_home -v 21)"
fi
exec ./gradlew "$@"
